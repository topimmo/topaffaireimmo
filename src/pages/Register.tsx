/**
 * Register Page
 * 
 * Routes to the unified AuthPage component in register mode
 */

import AuthPage from './AuthPage';

export default function Register() {
  return <AuthPage mode="register" />;
}
