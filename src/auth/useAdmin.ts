/**
 * Re-export of useAdmin hook from hooks directory
 * This file exists to support the required file structure in the problem statement
 * while maintaining compatibility with existing code.
 */
export { useAdmin } from '@/hooks/useAdmin';

